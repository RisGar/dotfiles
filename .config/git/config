[user]
  name = Rishab Garg
  email = me@rishab-garg.me
  signingkey = 7EC2233FD90AF4F1 
[core]
  editor = nvim
  autocrlf = false
  pager = delta

; ---- Delta settings ----
[interactive]
  diffFilter = delta --color-only
[delta]
  navigate = true
  dark = true
[merge]
  conflictstyle = zdiff3

; [diff]
;   colorMoved = default

; ---- GPG signing ----
[commit]
  gpgsign = true

[gpg]
  program = /opt/homebrew/bin/gpg

[tag]
  gpgSign = true

[filter "lfs"]
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true

; [pull]
;   rebase = false

[init]
  defaultBranch = master


[credential]
  helper = /usr/local/share/gcm-core/git-credential-manager

; ---- Other sources ----
[credential "https://artemis.cit.tum.de"]
  provider = generic
[credential "https://artemis.tum.de"]
  provider = generic
[credential "https://git.fs.tum.de"]
  provider = generic
